---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - tagList
  addAllPagesToCollections: true
layout: base.njk
permalink: /gallerytags/{{ tag }}/
eleventyComputed:
  title: "{{ tag | title }}"
  description: "Alle Gallerieeinträge mit Tag: “{{ tag }}”"
---
<h1>{{ tag | title }}</h1>

{% set postslist = collections[tag] %}
{% include "gallery-list.njk" %}
